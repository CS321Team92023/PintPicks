# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

variables:
- group: Variable group 01 - keystore signing

pool:
  vmImage: 'macos-latest'
  demands: xcode

steps:
- task: UseDotNet@2
  displayName: 'Install .NET sdk'
  inputs:
    packageType: sdk
    version: 6.0.x
    installationPath: $(Agent.ToolsDirectory)/dotnet

- task: CmdLine@2
  displayName: 'Install Maui Workload'
  inputs:
    script: 'dotnet workload install maui'

- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    projects: './src/PintPicks.sln'
    arguments: ' -c Release -f:net6.0-android /p:AndroidKeyStore=True /p:AndroidKeyStore=.\key.keystore /p:AndroidSigningStorePass=$(variables.keystore-password) /p:AndroidSigningKeyAlias=$(variables.key-alias) /p:AndroidSigningKeyPass=$(variables.key-password)'

- task: CopyFiles@2
  inputs:
    SourceFolder: '$(Agent.builddirectory)'
    Contents: '**/*-Signed.aab'
    TargetFolder: '$(Build.ArtifactStagingDirectory)'
    flattenFolders: true

- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'